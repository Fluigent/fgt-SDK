classdef fgt_TTL_MODE < uint8
    %FGT_TTL_MODE TTL port direction and pulse type
    
    enumeration
        DETECT_RISING_EDGE (0),
        DETECT_FALLING_EDGE (1),
        OUTPUT_PULSE_LOW (2),
        OUTPUT_PULSE_HIGH (3)
    end
    properties
    end
    
    methods
    end
    
end

